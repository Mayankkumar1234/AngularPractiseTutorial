

---------------------  How Force routing works in angular :------------------------

  if (forceNavigation) {
          return new Promise((resolve, reject) => {
            this.router
              .navigateByUrl('/', { skipLocationChange: true })
              .then(() =>
                this.router.navigate([fullPath], {
                  queryParams,
                  ...restNavigationOption,
                })
              )
              .then(resolve)
              .catch(reject);
          });
        } else {
          return this.router.navigate([fullPath], {
            queryParams: queryParams,
            ...restNavigationOption,
          });
        }

        Dependency injection in angular :-It is a design pattern that allow component to receives Dependency from another source rather than creating it in the component.
         - Has singleton instance means provideIn(root) - Single instance in the app.
         -Module Sope(Shared in lazy module) :- Module Injector(NgModule Providers)
         - Component Scope(New Instance per component) : Component injector need to inject in every component Providers

         @Injectable Mark as injectable -- 


- How DI works :-




How change detections works in angular compared it with react virtual dom :-

